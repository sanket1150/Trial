<%- include("header") -%>

  <div class="box" id="heading">
    <h1>
      <%= listTitle %>
    </h1>
  </div>

  <!-- <div class="box">
    <% for (let i=0; i<newListItems.length; i++) { %>
      <div class="item">
        <input type="checkbox">
        <p><%=  newListItems[i].name  %></p>
      </div>
      <% } %>

  </div> -->
  <form action="/delete/<%= listTitle %>" method="post">
    <div class="box">
      <% newListItems.forEach((item)=>{%>
        <div class="item">
          <input type="checkbox" onChange="this.form.submit()" value ="<%=item%>" name = "checkBox">
          <p>
            <%= item.name || item.item[0].name %>
          </p>
        </div>
        <% }) %>
    </div>
  </form>
  <div class="box">
    <form class="item" action="/add/<%= listTitle %>" method="post">
      <input type="text" name="newItem" placeholder="New Item" minlength="3" autocomplete="off" required>
      <button type="submit" name="list">+</button>
    </form>
  </div>

  <%- include("footer") -%>